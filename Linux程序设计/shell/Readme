# 说明（SHOW_explain)
## 使用说明
## 目前失败了。

目的就是尝试在Windows系统使用一些Linux特有的函数
例如：fork函数啥的，
但是，这个肯定是可以实现的。
理由可以看看Cygwin
[“All problems in computer science can be solved by another level of indirection（计算机科学领域的任何问题都可以通过增加一个间接的中间层来解决）”。](https://zhuanlan.zhihu.com/p/56692626)
<p id="diffusion_train" align="center">
<img src="https://pic1.zhimg.com/80/v2-c08d73d327e1ac976bde8a6c58bcd6e8_720w.webp" width=800/>
</p>
Cygwin就是在Windows中增加了一个中间层——兼容POSIX的模拟层，并在此基础上构建了大量Linux-like的软件工具。

****

所以一定可以的。

只是自己不行而已。哈。
哈。哈。