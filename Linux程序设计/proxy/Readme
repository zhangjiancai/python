# 来自知乎
# pigcha，可看pigcha帮助。但是，实测不行。哈😒

[使用脚本自动获取ip地址并设置代理](https://zhuanlan.zhihu.com/p/407559102)

# one more thing 
在使用```watt Toolkit```时，无法使用。
因为他使用的是反向代理。反向代理只适用于主机。
[而当我们使用WSL2时，wsl和Windows相当于两台处于一个网络中但分贝独立的主机（我们称这个网络为WSL网络）。在Windows主机上使用```ipconfig```我们可以看到Windows主机在WSL网络中的地址，相应的，在wsl中我们可以使用```ifconfig```命令来查看wsl在WSL网络中的地址。](https://zhuanlan.zhihu.com/p/407559102)
所以，哈哈。没戏。
而且，在使用Steam++的```host代理模式```时，还会在Windows里面包含各种```127.0.0.1```的地址。
然后，呢
因为：
```
# This file was automatically generated by WSL. To stop automatic generation of this file, add the following entry to /etc/wsl.conf:
# [network]
# generateHosts = false
```

还会让原本wsl可以访问的无法访问。所以，

要在```/etc/```文件夹下创建```wsl.conf```
内容如上。

```
 [network]
 generateHosts = false
```

# 好了就是这样